# Core
pillow>=10.0.0
pystray>=0.19.0
pywin32>=306
psutil>=5.9.0

# Voice commands (pick one)
SpeechRecognition>=3.10.0
pyaudio>=0.2.13
# openai-whisper>=20231117  # offline STT, optional

# Notifications (pick one)
winotify>=1.1.3
# win10toast>=0.9  # alternative

# Spotify Web API (optional)
# No extra deps needed — uses urllib only

# Google Workspace (optional)
# google-api-python-client>=2.100.0
# google-auth-httplib2>=0.1.1
# google-auth-oauthlib>=1.1.0

# Build
pyinstaller>=6.0.0